
import { GameStateProviderCore, useGameStateContextCore } from './GameStateProviderCore';

// Export the provider and context hook with their original names
export const GameStateProvider = GameStateProviderCore;
export const useGameStateContext = useGameStateContextCore;
